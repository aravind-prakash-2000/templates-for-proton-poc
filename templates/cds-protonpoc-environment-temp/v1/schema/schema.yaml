schema:
  format:
    openapi: "3.0.0"
  environment_input_type: "EnvironmentInput"
  types:
    EnvironmentInput:
      type: object
      description: "Input properties for my environment"
      properties:
        bucketname:
          type: string
          description: "name of bucket"
          default: cds-protonpoc-s3bucket
          minLength: 1
          maxLength: 100
